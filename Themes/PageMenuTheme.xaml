<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <Style BasedOn="{StaticResource {x:Type ToggleButton}}"
           TargetType="{x:Type RadioButton}"
           x:Key="PageMenuTheme">
        <Style.Setters>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border Background="Transparent" BorderBrush="#FF4E669E" BorderThickness="0,0,0,3" Width="90">
                            <TextBlock Text="{TemplateBinding Property=ContentControl.Content}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Light" Margin="0,15,0,0" FontSize="18" FontFamily="Guttman Aharoni" Foreground="#FF4E669E"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style.Setters>

    </Style>
    
    
</ResourceDictionary>